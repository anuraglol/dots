#!/bin/bash

url="$1"

browser_class="app.zen_browser.zen"

if hyprctl clients | grep -i "$browser_class" > /dev/null; then
    hyprctl dispatch focuswindow "class:$browser_class"
    sleep 0.1
    xdg-open "$url"
else
    xdg-open "$url"
fi
